import React from 'react';
//import './auth.styles.scss';

const Layout = ({children}) => {
    return (
        <div>
            <div>Layout</div>
            <div>{children}</div>
        </div>    
    );
}

export default Layout; 